services:
  - type: web
    name: ofertas-staffing
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn Back_end.Main:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
